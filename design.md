# 設計書

## 開発方針

本プロジェクトでは、将棋の棋譜を扱うための基本モデルと、局面から取得できる特徴量を段階的に実装する。

戦型名を直接判定することよりも、まず局面から取得できる特徴量を扱えるようにすることを優先する。

---

## バージョンごとの方針

### Ver.0.1 基本モデル・局面再構築

将棋の棋譜と局面を扱うための基本モデルを実装する。

以下のクラスを作成する。

- `Side`：先手・後手を表す
- `PieceType`：駒種を表す
- `Piece`：駒の所属、駒種、成り状態を表す
- `Square`：盤上の座標を表す
- `Move`：1手の指し手を表す
- `Position`：盤面、持ち駒、手番などの1局面を表す
- `GameRecord`：1局分の指し手列を保持する

`Move`には通常の駒移動だけでなく、駒取り、成り、駒打ちを表現できるようにする。

`GameRecord`に記録された指し手を順番に`Position`へ適用することで、棋譜の任意の手数まで局面を再構築できるようにする。

また、実際のKIF棋譜4局の序盤20手を用いたJUnitテストを作成し、指し手の適用と局面再構築が正しく行われることを確認する。

Ver.0.1ではKIFファイルそのものの読み込みは行わず、指し手は`Move`として直接与える。KIFファイルの読み込みはVer.0.2で実装する。

### Ver.0.2 KIF棋譜読み込み

KIFファイルを読み込み、指し手を`Move`へ変換して`GameRecord`を生成できるようにする。

Ver.0.1で実装した局面再構築機能と接続し、実際のKIF棋譜から任意の手数まで局面を再生できることをテストする。

KIFの全仕様を対象とするのではなく、まずはテストに使用する棋譜で必要となる表記を扱えることを目標とする。

### Ver.0.3 中飛車判定

局面から取得した特徴量を利用して、中飛車の形を判定できるようにする。

### Ver.0.4 相手の局面特徴抽出

相手の駒組みなど、戦型や局面分類に利用できる特徴を抽出する。

### Ver.0.5 USI対応

将棋エンジンとの連携に必要なUSI形式の入出力を扱えるようにする。

### Ver.0.6 評価値解析

将棋エンジンの評価値を利用した局面解析を行えるようにする。

### Ver.0.7 棋譜分類

棋譜や局面を分類するための機能を実装する。

### Ver.0.8 データ保存

必要な棋譜・局面データを保存できる仕組みを実装する。

### Ver.0.9 練習問題生成

蓄積した棋譜や局面情報を利用して、中飛車の練習問題を生成できるようにする。

---

## 基本モデル

### Side

先手・後手を表す。

### PieceType

駒の種類を表す。駒の名称には日本語名称をローマ字化した名前を使用する。

```text
FU
KYO
KEI
GIN
KIN
KAKU
HI
OU
```

王と玉は同じ`OU`として扱う。

### Piece

駒の所属、駒種、成り状態を表す。

### Square

盤上の座標を表す。

### Move

1手の指し手を表す。

通常の駒移動では移動元と移動先を持つ。駒打ちでは`from == null`として表現する。

### Position

1局面を表す。盤面、持ち駒、手番などを保持する。

外部からは盤面の取得や指し手の適用を行えるAPIを提供し、内部の盤面表現には依存しない設計とする。

`Position`自身は棋譜の履歴を保持しない。

### GameRecord

`GameRecord`は1局分の指し手列を表す。

Ver.0.1では、以下の情報を保持する。

- `List<Move> moves`

局面そのものを履歴として保持するのではなく、指し手列を基本データとする。必要な局面は初期局面から指し手を順番に適用することで再構築する。

#### 将来追加する可能性のある情報

以下の情報については、棋譜の分類や局面抽出などの機能を実装する段階で、必要性を確認した上で追加する。

- 対局者情報
- 対局日時
- 対局結果
- 対局場所・大会などの棋譜情報
- その他、棋譜の分類や局面抽出に必要となる情報

現時点ではこれらを`GameRecord`に持たせず、必要になった段階で設計を見直す。

---

## 局面特徴

戦型判定や局面分類に必要な特徴は`Position`から分離して扱う。

```text
Position
    ↓
PositionFeatureExtractor
    ↓
PositionFeatures
```

`Position`は将棋の局面そのものを表し、戦型判定などの知識を持たない。

`PositionFeatureExtractor`が`Position`から必要な特徴を抽出し、`PositionFeatures`として提供する。

---

## 内部表現

盤面の内部表現は、まず分かりやすいデータ構造で実装する。性能上の問題が明らかになった場合はBitboardなどへの変更を検討する。

外部APIは内部表現に依存しないようにすることで、内部実装を変更しても他のクラスへの影響を抑える。
