# Nakabisha Trainer 設計書

## 1. プロジェクトの目的

Nakabisha Trainer は、自分の中飛車の棋譜を蓄積・分析し、相手の駒配置や局面ごとの評価値から、自分が苦手としている局面や相手の構えを発見するための将棋支援ツールである。

将棋エンジンそのものの強さを競うのではなく、既存の将棋エンジンを活用して中飛車の研究を支援することを目的とする。

最終的には、自分の棋譜から苦手局面を発見し、その局面を練習問題として利用できるようにする。

## 2. 基本方針

- 戦型名を完全に判定することよりも、局面から取得できる特徴量を扱うことを優先する。
- 中飛車判定は、序盤の数手を見たうえで飛車が5筋へ振られていることなど、比較的単純な条件から開始する。
- 相手の戦型を完全に分類することは目標とせず、玉・銀・飛車・角・歩などの一部の駒配置を特徴量として抽出する。
- 元棋譜を基本データとして保存し、棋譜から再計算可能な情報は必要に応じて再生成する。
- 全局面の解析結果を無条件に保存するのではなく、評価値が大きく変化した重要局面など、研究に必要な情報を選択して保存する。
- 性能上の問題が実際に発生するまでは、分かりやすさと保守性を優先する。
- ただし、盤面表現などの内部実装を将来的に Bitboard 等へ置き換えられるよう、上位層から実装詳細を隠蔽する。

## 3. Ver.0.1 の実装内容

将棋の棋譜と局面を扱うための基本モデルを実装した。

以下のクラスを作成した。

- `Side`：先手・後手を表す
- `PieceType`：駒種を表す
- `Piece`：駒の所属、駒種、成り状態を表す
- `Square`：盤上の座標を表す
- `Move`：1手の指し手を表す
- `Position`：盤面、持ち駒、手番などの1局面を表す
- `GameRecord`：1局分の指し手列を保持する

`Move`には通常の駒移動だけでなく、駒取り、成り、駒打ちを表現できるようにした。

`GameRecord`に記録された指し手を順番に`Position`へ適用することで、棋譜の任意の手数まで局面を再構築できるようにした。

また、実際のKIF棋譜4局の序盤20手を用いたJUnitテストを作成し、指し手の適用と局面再構築が正しく行われることを確認した。

Ver.0.1ではKIFファイルそのものの読み込みは行わず、指し手は`Move`として直接与える。KIFファイルの読み込みはVer.0.2で実装する。

## 4. Ver.0.2 KIF棋譜読み込み

### 4.1 目的

KIF形式の棋譜ファイルを読み込み、指し手を後続の`Move`変換処理へ渡せる形にする。

この段階ではKIFから`Move`への変換や`GameRecord`の生成は行わず、KIFファイルから指し手行を抽出するところまでを担当する。

### 4.2 基本構成

```text
KIFファイル
    ↓
KifReader.read(Path)
    ↓
List<String>
    ↓
KifMoveConverter
    ↓
List<Move>
```

`KifReader`と`KifMoveConverter`は直接依存させず、それぞれの責務を分離する。

### 4.3 KifReader

`KifReader`はKIFファイルの読み込みを担当する。

以下の静的メソッドを公開APIとし、KIFファイルから指し手行を抽出して返す。

```java
static List<String> read(Path path)
```

指し手行の文字列は加工せず、消費時間などの付加情報を含めてKIFファイルに記述された状態のまま返す。

KIFの指し手の意味の解析や`Move`への変換は`KifMoveConverter`の責務とする。

### 4.4 KIFの読み込み範囲

現時点では、`Move`の生成に必要となる指し手関連の情報のみを扱う。

対象には以下を含む。

- 指し手番号
- 通常の指し手
- `同`
- 移動元
- 成り・不成
- 駒打ち

一方、以下の情報は現時点では利用せず読み飛ばす。

- 対局者・日時・棋戦などのヘッダ情報
- コメント
- 空行・見出し
- 消費時間
- 勝敗・投了などの終局情報

ただし、これらを将来的に利用できるよう、KIFの構造上それらと指し手行を区別できるようにする。

### 4.5 文字コード

`.kif`ファイルはKIF標準仕様に準拠し、Windows-31J系（Shift-JIS）として読み込む。

`.kifu`形式（UTF-8）は現時点では対象外とするが、将来的に対応できる構造を意識する。

### 4.6 エラー処理

エラーは原因によって区別する。

| 状況 | 例外 |
|---|---|
| ファイルの読み込みに失敗 | `IOException` |
| KIFの構造・形式が不正または不完全 | `KifParseException` |
| 正しいKIFだが未対応の表現 | `UnsupportedKifException` |

未対応の指し手表現については、主に`KifMoveConverter`側で検出する。

### 4.7 指し手行の加工

`KifReader`では指し手行を解釈・加工しない。

消費時間などの付加情報も含め、KIFに記述されている文字列をそのまま`List<String>`に格納する。KIF文字列の解析や不要な情報の除去は`KifMoveConverter`側で行う。

### 4.8 指し手以外の行

ヘッダ、空行、コメント、見出し、終局情報などは現時点では読み飛ばす。

ただし、指し手行とそれ以外を識別できるようにし、将来的に必要になった場合に利用できる余地を残す。

### 4.9 テスト方針

Ver.0.2では`KifReader`の指し手行抽出処理をテストする。

以下を検証する。

- 指し手行だけが抽出される
- ヘッダなど読み飛ばすべき行が含まれない
- 必要な指し手行が欠落しない
- 指し手の順序が維持される
- 指し手行の文字列が加工されていない

`Move`への変換や局面の再現は、この段階ではテスト対象としない。

### 4.10 不完全KIFと未対応KIF

「KIFとして壊れている／必要な情報が欠けている」ものと、「KIFとして正しいが現在のプログラムが対応していない」ものを区別する。

前者は`KifParseException`、後者は`UnsupportedKifException`として扱う。

### 4.11 将来の拡張

現時点では必要最低限の機能のみ実装する。将来の機能を先回りして実装するのではなく、クラス・メソッドの責務を適切に分離することで、必要になったときに拡張しやすい構造を意識する。

将来的に必要となった場合には、`.kifu`対応、棋譜情報、勝敗・終局情報、消費時間などを追加する。

## 5. 基本モデル

### 5.1 `Side`

先手・後手を表す。

```text
SENTE
GOTE
```

### 5.2 `PieceType`

駒の種類を表す。

コード上ではチェス由来の `PAWN` や `ROOK` 等ではなく、将棋の日本語名称をローマ字表記した名前を使用する。

```text
FU
KYO
KEI
GIN
KIN
KAKU
HI
OU
```

王と玉は同じ `OU` として扱う。

成りは駒種そのものには含めず、`Piece` の `promoted` で表現する。

### 5.3 `Piece`

盤上に存在する具体的な駒を表す。

```text
Piece
├── Side side
├── PieceType type
└── boolean promoted
```

例えば、先手の成っていない飛車は `SENTE + HI + false`、先手の成銀は `SENTE + GIN + true` とする。

### 5.4 `Square`

9×9 の盤上の位置を表す。

`Square(file, rank)` の `file` は棋譜で使用する「筋」（1〜9）、`rank` は「段」（1〜9）を表す。したがって、例えば `Square(2, 8)` は棋譜上の「２八」、`Square(8, 2)` は「８二」に対応する。

この座標系では、初期配置の飛車と角は以下の位置になる。

```text
後手飛車: (8, 2)
後手角  : (2, 2)
先手角  : (8, 8)
先手飛車: (2, 8)
```

`Square` は筋・段を1〜9の範囲に制限し、盤面配列の内部インデックスへの変換も提供する。

### 5.5 `Move`

1手の指し手を表す。

通常の駒の移動と駒打ちを区別するため、駒打ちの場合は `from` を `null` とする。

概念上は以下を保持する。

```text
Move
├── Square from
├── Square to
├── PieceType pieceType
└── boolean promotion
```

`from != null` の場合は通常の駒の移動を表し、`from == null` の場合は持ち駒から `to` へ駒を打つ指し手を表す。

`from == null` の場合でも、`to` と `pieceType` は必ず保持する。駒打ちでは `promotion` は `false` とする。

### 5.6 `Position`

ある時点の局面を表す。主な状態は以下のとおり。

```text
Position
├── 盤上の駒
├── 先手の持ち駒
├── 後手の持ち駒
└── 手番
```

外部からは盤面の取得や指し手の適用を行えるAPIを提供し、内部の盤面表現には依存しない設計とする。

初期局面を生成する `Position.initial()` を用意する。初期局面では盤上の駒を将棋の標準初期配置に設定し、先手・後手の持ち駒を空、手番を先手とする。

`Position` 自身は棋譜の履歴を保持しない。

### 5.7 `GameRecord`

`GameRecord`は1局分の指し手列を表す。

Ver.0.1では、以下の情報を保持する。

- `List<Move> moves`

局面そのものを履歴として保持するのではなく、指し手列を基本データとする。必要な局面は初期局面から指し手を順番に適用することで再構築する。

#### 将来追加する可能性のある情報

以下の情報については、棋譜の分類や局面抽出などの機能を実装する段階で、必要性を確認した上で追加する。

- 対局者情報
- 対局日時
- 対局結果
- 対局場所・大会などの棋譜情報
- その他、棋譜の分類や局面抽出に必要となる情報

現時点ではこれらを `GameRecord` に持たせず、必要になった段階で設計を見直す。

## 6. Position の変更方針

基本モデルとして扱いやすい `Position` を提供しつつ、大量の棋譜を処理する場合に備えて、将来的には `MutablePosition` のような再生・解析用の内部モデルを導入できる構造を想定する。

必要に応じて、

```text
apply(move)
undo(move)
```

のような操作で盤面を使い回せるようにする。

これにより、すべての手で新しい盤面を完全に複製する実装を避けられるようにする。

## 7. 盤面表現と性能

Ver.0.1 では、理解しやすい単純な盤面表現を優先する。

ただし、`Position` の利用者が内部実装に依存しないようにし、必要になった場合に以下のような実装へ変更できるようにする。

```text
単純な配列等
    ↓
Bitboard
```

将棋盤は81マスあるため、オセロのように単一の64bit値だけで盤面を表すことはできないが、複数の整数を組み合わせた Bitboard 表現は可能である。

将来的に自前の探索エンジンを作る場合や大量の局面を高速処理する必要が生じた場合に検討する。

## 8. 局面特徴の分離

局面そのものを表す `Position` に、中飛車判定や戦型判定のロジックを直接持たせない。

将来的に以下のような分離を行う。

```text
Position
    ↓
PositionFeatureExtractor
    ↓
PositionFeatures
```

これにより、戦型判定や特徴量の定義を変更しても、基本的な局面モデルを変更せずに済むようにする。

## 9. 中飛車判定

中飛車判定は、最初から完全な戦型認識を目指さない。

例えば先手の場合、序盤に飛車が5筋へ移動していること、5筋の歩の状態など、複数の単純な条件を組み合わせて中飛車候補・中飛車と判定する。

相手側については、戦型名を完全に分類するのではなく、玉や銀など一部の駒配置を特徴として抽出する。

## 10. 将棋エンジン連携

将棋エンジンそのものは実装せず、将来的に USI を利用して既存エンジンと連携する。

```text
Nakabisha Trainer
        ↓
      USI
        ↓
既存将棋エンジン
```

取得した評価値や候補手を、棋譜の局面分析に利用する。

## 11. 重要局面と苦手分析

全局面のエンジン解析結果を無条件に保存するのではなく、評価値が大きく変化した局面などを重要局面として抽出する。

将来的には、例えば以下のような特徴と評価値変化を集計する。

```text
中飛車
+ 相手居飛車
+ 相手銀6四
+ 相手玉8八
        ↓
評価値が大きく落ちやすい
```

この集計結果から、自分が苦手とする局面や相手の構えを発見する。

## 12. データ保存

基本データとして棋譜を保存する。

局面から再計算可能な情報は、原則として元棋譜から再生成できるようにする。

将来的には SQLite を利用して棋譜・重要局面・解析結果等を保存する。

また、同一局面の再解析を避けるため、将来的に Zobrist Hash を利用した解析キャッシュを導入する。

```text
Position
    ↓
Zobrist Hash
    ↓
Analysis Cache
```

## 13. 将来的な発展

```text
Ver.0.1  基本モデル・局面再構築
Ver.0.2  KIF棋譜読み込み
Ver.0.3  中飛車判定・特徴量抽出
Ver.0.4  エンジン連携
Ver.0.5  重要局面抽出
Ver.0.6  苦手局面分析
Ver.0.7  データベース化
Ver.0.8  練習問題生成
```
